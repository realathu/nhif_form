export interface CourseEnum {
  id: string
  name: string
  faculty: string
  duration: number
  active: boolean
}

export interface AdmissionDateEnum {
  id: string
  date: string
  academicYear: string
  active: boolean
}
